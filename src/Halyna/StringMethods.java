package Halyna;

public class StringMethods {
    public static void main(String[] args) {

        // charAt(index)
        String s = "Python";
     char n = s.charAt(3);

        System.out.println(n);

        //length() , returns int, total number of characters
        //last index length()-1
        System.out.println(s.length());

        //toLowerCase(): returns a new String
        s = s.toLowerCase();
        System.out.println(s);

        //toUpperCase(): returns a new String
        s = s.toUpperCase();

        //trim()

        //indexOf(str): returns int, index number of first occured character

        //lastIndexOf(str)

        //replace(oldValue, newValue) replacing ALL existing

        //replaceFirst(oldValue, newValue) replacing FIRST existing

        //substring(beginningIndex, endingIndex) (ending index is excluding)  1 -> 3 + 1 (4)

        //substring (beginning index) from the given index till the end

        //repeat(count) repeats the String for given number of time

        //equals(str) return boolean

        //equalsIgnoreCase(str)

        //contains(str)

        //startWith(str)

        //endsWith()





    }
}
